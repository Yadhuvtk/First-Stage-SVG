numpy
opencv-python
Pillow
PyYAML
pypotrace

# Optional: ML pre-processing pipeline (--preprocess flag)
# Requires CUDA 12.x for GPU acceleration (RTX 5090 tested)
realesrgan
basicsr
segment-anything-2
scikit-image
# torch and torchvision must be installed separately with the correct CUDA version:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124